<% title t('.title') %>
<div class="govuk-body">
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-full">
      <%= t('.explainer_html') %>
      <% @iframe_urls.each do |url| %>
        <%= render 'dashboard_iframe', url: url%>
      <% end %> 
    </div>
  </div>
</div>
